# BIRD 2.x configuration for rs-golden
# Generated by IXForge Core at <fijo>
# Config hash: <fijo>
# DO NOT EDIT MANUALLY - this file is managed by IXForge

timeformat base     iso long;
timeformat log      iso long;
timeformat protocol iso long;
timeformat route    iso long;

log syslog all;
log stderr { error, fatal };

define routeserverasn = 65000;
define routeserveraddress4 = 192.0.2.250;
define routeserveraddress6 = 2001:db8::250;

router id 192.0.2.250;

# ignore interface up/down events
protocol device { }

# Communities del esquema euro-ix
# https://github.com/euro-ix/rs-workshop-july-2017/wiki/Route-Server-BGP-Community-usage

# Filtradas: el pipe hacia master descarta todo lo que lleve (routeserverasn, 1101, *)
define IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG      = ( routeserverasn, 1101, 1  );
define IXP_LC_FILTERED_PREFIX_LEN_TOO_SHORT     = ( routeserverasn, 1101, 2  );
define IXP_LC_FILTERED_BOGON                    = ( routeserverasn, 1101, 3  );
define IXP_LC_FILTERED_BOGON_ASN                = ( routeserverasn, 1101, 4  );
define IXP_LC_FILTERED_AS_PATH_TOO_LONG         = ( routeserverasn, 1101, 5  );
define IXP_LC_FILTERED_AS_PATH_TOO_SHORT        = ( routeserverasn, 1101, 6  );
define IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS     = ( routeserverasn, 1101, 7  );
define IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP     = ( routeserverasn, 1101, 8  );
define IXP_LC_FILTERED_IRRDB_PREFIX_FILTERED    = ( routeserverasn, 1101, 9  );
define IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED = ( routeserverasn, 1101, 10 );
define IXP_LC_FILTERED_PREFIX_NOT_IN_ORIGIN_AS  = ( routeserverasn, 1101, 11 );
define IXP_LC_FILTERED_RPKI_UNKNOWN             = ( routeserverasn, 1101, 12 );
define IXP_LC_FILTERED_RPKI_INVALID             = ( routeserverasn, 1101, 13 );
define IXP_LC_FILTERED_TRANSIT_FREE_ASN         = ( routeserverasn, 1101, 14 );
define IXP_LC_FILTERED_TOO_MANY_COMMUNITIES     = ( routeserverasn, 1101, 15 );

# Informativas
define IXP_LC_INFO_RPKI_VALID       = ( routeserverasn, 1000, 1  );
define IXP_LC_INFO_RPKI_UNKNOWN     = ( routeserverasn, 1000, 2  );
define IXP_LC_INFO_RPKI_NOT_CHECKED = ( routeserverasn, 1000, 3  );
define IXP_LC_INFO_RPKI_INVALID     = ( routeserverasn, 1000, 4  );

define IXP_LC_INFO_IRRDB_VALID         = ( routeserverasn, 1001, 1  );
define IXP_LC_INFO_IRRDB_NOT_CHECKED   = ( routeserverasn, 1001, 2  );
define IXP_LC_INFO_IRRDB_MORE_SPECIFIC = ( routeserverasn, 1001, 3  );

define IXP_LC_INFO_IRRDB_FILTERED_LOOSE  = ( routeserverasn, 1001, 1000 );
define IXP_LC_INFO_IRRDB_FILTERED_STRICT = ( routeserverasn, 1001, 1001 );
define IXP_LC_INFO_IRRDB_PREFIX_EMPTY    = ( routeserverasn, 1001, 1002 );

define IXP_LC_INFO_SAME_AS_NEXT_HOP = ( routeserverasn, 1001, 1200 );

# forma large del tipo de miembro, para IXPs con ASN de 4 bytes que no entra
# en una community estandar
define IXP_LC_INFO_MEMBER_TYPE_BASE = ( routeserverasn, 1002, 0 );
# Martians, listas del patron euro-ix

define MARTIANS_V4 = [
    0.0.0.0/32-,            # rfc5735 Special Use IPv4 Addresses
    0.0.0.0/0{0,7},         # rfc1122 3.2.1.3
    10.0.0.0/8+,            # rfc1918
    100.64.0.0/10+,         # rfc6598 Shared Address Space
    127.0.0.0/8+,           # rfc1122 loopback
    169.254.0.0/16+,        # rfc3927 link-local
    172.16.0.0/12+,         # rfc1918
    192.0.0.0/24+,          # rfc6890
    192.0.2.0/24+,          # rfc5737 TEST-NET-1
    192.168.0.0/16+,        # rfc1918
    198.18.0.0/15+,         # rfc2544 benchmarking
    198.51.100.0/24+,       # rfc5737 TEST-NET-2
    203.0.113.0/24+,        # rfc5737 TEST-NET-3
    224.0.0.0/4+,           # rfc1112 multicast
    240.0.0.0/4+            # rfc6890 reservado
];

define MARTIANS_V6 = [
    ::/0,                   # default
    ::/96,                  # IPv4-compatible, deprecado por rfc4291
    ::/128,                 # unspecified
    ::1/128,                # loopback
    ::ffff:0.0.0.0/96+,     # IPv4-mapped
    ::224.0.0.0/100+,
    ::127.0.0.0/104+,
    ::0.0.0.0/104+,
    ::255.0.0.0/104+,
    0000::/8+,
    0200::/7+,              # OSI NSAP-mapped, deprecado por rfc4048
    3ffe::/16+,             # 6bone
    3fff::/20+,             # rfc9637 documentacion
    2001:db8::/32+,         # rfc3849 documentacion
    2002:e000::/20+,        # 6to4 invalido (multicast)
    2002:7f00::/24+,        # 6to4 invalido (loopback)
    2002:0000::/24+,        # 6to4 invalido (default)
    2002:ff00::/24+,
    2002:0a00::/24+,        # 6to4 invalido (10/8)
    2002:ac10::/28+,        # 6to4 invalido (172.16/12)
    2002:c0a8::/32+,        # 6to4 invalido (192.168/16)
    fc00::/7+,              # rfc4193 ULA
    fe80::/10+,             # link-local
    fec0::/10+,             # site-local, deprecado por rfc3879
    ff00::/8+               # multicast
];
# Funciones comunes

function avoid_martians4() {
    if net ~ MARTIANS_V4 then return false;
    return true;
}

function avoid_martians6() {
    if net ~ MARTIANS_V6 then return false;
    return true;
}

function honor_graceful_shutdown() {
    if (65535, 0) ~ bgp_community then {
        bgp_local_pref = 0;
    }
}
# ASNs transit-free, inspirado en http://bgpfilterguide.nlnog.net/guides/no_transit_leaks/
# Esta lista envejece: es editable a proposito para poder actualizarla sin un release

define TRANSIT_ASNS = [ 174, 701, 1299, 2914, 3257, 3320, 3356, 3491, 4134, 5511, 6453, 6461, 6762, 6830, 7018 ];

function filter_has_transit_path()
int set transit_asns;
{
    transit_asns = TRANSIT_ASNS;
    if (bgp_path ~ transit_asns) then {
        bgp_large_community.add( IXP_LC_FILTERED_TRANSIT_FREE_ASN );
        return true;
    }

    return false;
}
# Control de anuncio selectivo por communities
# Se evalua en el pipe master -> tabla del peer, o sea decide a quien se le
# anuncia cada ruta segun lo que el miembro que la origino haya pedido

function ixp_community_filter(int peerasn)
{
    # las rutas que no vienen de BGP no se propagan
    if !(source = RTS_BGP) then
        return false;

    # 1. no anunciar a un peer especifico, maxima prioridad
    if (peerasn <= 65535) && (0, peerasn) ~ bgp_community then
        return false;
    if (routeserverasn, 0, peerasn) ~ bgp_large_community then
        return false;

    # 2. no anunciar a nadie, con excepciones
    bool deny_all_std = (0, routeserverasn) ~ bgp_community;
    bool deny_all_lrg = (routeserverasn, 0, 0) ~ bgp_large_community;

    if deny_all_std || deny_all_lrg then {
        # excepcion: anunciar a todos
        if (routeserverasn, routeserverasn) ~ bgp_community then
            return true;
        if (routeserverasn, 1, 0) ~ bgp_large_community then
            return true;

        # excepcion: anunciar a este peer
        if (peerasn <= 65535) && (routeserverasn, peerasn) ~ bgp_community then
            return true;
        if (routeserverasn, 1, peerasn) ~ bgp_large_community then
            return true;

        return false;
    }

    # 3. por defecto se anuncia
    return true;
}
# El unico lugar donde se descarta: todo lo marcado como filtrado muere aca
filter f_export_to_master
{
    if bgp_large_community ~ [( routeserverasn, 1101, * )] then reject;
    accept;
}
# Validacion RPKI via RTR
roa4 table roa_v4;
roa6 table roa_v6;

protocol rpki rpki_routinator {
    roa4 { table roa_v4; };
    roa6 { table roa_v6; };
    remote "10.0.0.1" port 3323;
}

template bgp tb_rsclient_v4 {
    local as routeserverasn;
    source address routeserveraddress4;
    passive yes;

    # le da tiempo al RTR a poblarse antes de que levanten las sesiones
    connect delay time 30;

    # pass through de communities well-known de rfc1997
    interpret communities off;

    ipv4 {
        # sin import table, cuando cambian o expiran las ROAs BIRD no puede
        # reevaluar el filtro de import sobre las rutas ya recibidas sin pedirle
        # un route refresh al peer
        import table on;
        export all;
    };

    rs client;
}
# Miembro AS25152 (AS25152) en 192.0.2.10
ipv4 table t_KROOT_192_0_2_10_v4;

filter f_import_KROOT_192_0_2_10_v4
ip set allips;
int set allas;
{
    if ( net ~ [ 0.0.0.0/0{25,32} ] ) then {
        bgp_large_community.add( IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG );
        accept;
    }

    if !(avoid_martians4()) then {
        bgp_large_community.add( IXP_LC_FILTERED_BOGON );
        accept;
    }

    if( bgp_path.len < 1 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_SHORT );
        accept;
    }

    if (bgp_path.first != 25152 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS );
        accept;
    }

    allips = [ 192.0.2.10 ];

    if !( from = bgp_next_hop ) then {
        if( bgp_next_hop ~ allips ) then {
            bgp_large_community.add( IXP_LC_INFO_SAME_AS_NEXT_HOP );
        } else {
            bgp_large_community.add( IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP );
            accept;
        }
    }

    if filter_has_transit_path() then accept;

    if( bgp_path.len > 64 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_LONG );
        accept;
    }

    allas = [ 25152 ];

    if !(bgp_path.last_nonaggregated ~ allas) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED );
        accept;
    }

    if ( roa_check(roa_v4, net, bgp_path.last) = ROA_VALID ) then {
        bgp_large_community.add( IXP_LC_INFO_RPKI_VALID );
    } else {
        if ( roa_check(roa_v4, net, bgp_path.last) = ROA_INVALID ) then {
            bgp_large_community.add( IXP_LC_INFO_RPKI_INVALID );
            bgp_large_community.add( IXP_LC_FILTERED_RPKI_INVALID );
        } else {
            bgp_large_community.add( IXP_LC_INFO_RPKI_UNKNOWN );
        }
    }

    bgp_large_community.add( IXP_LC_INFO_IRRDB_NOT_CHECKED );

    honor_graceful_shutdown();

    bgp_community.add( (routeserverasn, 65270) );

    accept;
}

# el export strippea nuestras propias communities de filtrado y looking glass.
# Las dos formas: dejar pasar las estandar (routeserverasn, *) filtra menos de
# lo que el operador cree, porque las de control de anuncio y las de marca de
# upstream llegarian al miembro
filter f_export_KROOT_192_0_2_10_v4
{
    bgp_large_community.delete( [( routeserverasn, *, * )] );
    bgp_community.delete( [( routeserverasn, * )] );
    accept;
}

protocol bgp pb_KROOT_192_0_2_10_v4 from tb_rsclient_v4 {
    description "Miembro AS25152";
    neighbor 192.0.2.10 as 25152;

    ipv4 {
        import limit 6 action restart;
        import filter f_import_KROOT_192_0_2_10_v4;
        table t_KROOT_192_0_2_10_v4;
        export filter f_export_KROOT_192_0_2_10_v4;
    };
}

protocol pipe pp_KROOT_192_0_2_10_v4 {
    description "Pipe for Miembro AS25152";
    table master4;
    peer table t_KROOT_192_0_2_10_v4;
    import filter f_export_to_master;
    export where ixp_community_filter(25152);
}
# Miembro AS61455 (AS61455) en 192.0.2.16
ipv4 table t_CDN_192_0_2_16_v4;

filter f_import_CDN_192_0_2_16_v4
ip set allips;
int set allas;
{
    if ( net ~ [ 0.0.0.0/0{25,32} ] ) then {
        bgp_large_community.add( IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG );
        accept;
    }

    if !(avoid_martians4()) then {
        bgp_large_community.add( IXP_LC_FILTERED_BOGON );
        accept;
    }

    if( bgp_path.len < 1 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_SHORT );
        accept;
    }

    if (bgp_path.first != 61455 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS );
        accept;
    }

    allips = [ 192.0.2.16, 192.0.2.18 ];

    if !( from = bgp_next_hop ) then {
        if( bgp_next_hop ~ allips ) then {
            bgp_large_community.add( IXP_LC_INFO_SAME_AS_NEXT_HOP );
        } else {
            bgp_large_community.add( IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP );
            accept;
        }
    }

    if filter_has_transit_path() then accept;

    if( bgp_path.len > 64 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_LONG );
        accept;
    }

    allas = [ 61455 ];

    if !(bgp_path.last_nonaggregated ~ allas) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED );
        accept;
    }

    if ( roa_check(roa_v4, net, bgp_path.last) = ROA_VALID ) then {
        bgp_large_community.add( IXP_LC_INFO_RPKI_VALID );
    } else {
        if ( roa_check(roa_v4, net, bgp_path.last) = ROA_INVALID ) then {
            bgp_large_community.add( IXP_LC_INFO_RPKI_INVALID );
            bgp_large_community.add( IXP_LC_FILTERED_RPKI_INVALID );
        } else {
            bgp_large_community.add( IXP_LC_INFO_RPKI_UNKNOWN );
        }
    }

    bgp_large_community.add( IXP_LC_INFO_IRRDB_NOT_CHECKED );

    honor_graceful_shutdown();

    bgp_community.add( (routeserverasn, 65250) );

    accept;
}

# el export strippea nuestras propias communities de filtrado y looking glass.
# Las dos formas: dejar pasar las estandar (routeserverasn, *) filtra menos de
# lo que el operador cree, porque las de control de anuncio y las de marca de
# upstream llegarian al miembro
filter f_export_CDN_192_0_2_16_v4
{
    bgp_large_community.delete( [( routeserverasn, *, * )] );
    bgp_community.delete( [( routeserverasn, * )] );
    accept;
}

protocol bgp pb_CDN_192_0_2_16_v4 from tb_rsclient_v4 {
    description "Miembro AS61455";
    neighbor 192.0.2.16 as 61455;

    ipv4 {
        import filter f_import_CDN_192_0_2_16_v4;
        table t_CDN_192_0_2_16_v4;
        export filter f_export_CDN_192_0_2_16_v4;
    };
}

protocol pipe pp_CDN_192_0_2_16_v4 {
    description "Pipe for Miembro AS61455";
    table master4;
    peer table t_CDN_192_0_2_16_v4;
    import filter f_export_to_master;
    export where ixp_community_filter(61455);
}
# Miembro AS61455 (AS61455) en 192.0.2.18
ipv4 table t_CDN_192_0_2_18_v4;

filter f_import_CDN_192_0_2_18_v4
ip set allips;
int set allas;
{
    if ( net ~ [ 0.0.0.0/0{25,32} ] ) then {
        bgp_large_community.add( IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG );
        accept;
    }

    if !(avoid_martians4()) then {
        bgp_large_community.add( IXP_LC_FILTERED_BOGON );
        accept;
    }

    if( bgp_path.len < 1 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_SHORT );
        accept;
    }

    if (bgp_path.first != 61455 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS );
        accept;
    }

    allips = [ 192.0.2.16, 192.0.2.18 ];

    if !( from = bgp_next_hop ) then {
        if( bgp_next_hop ~ allips ) then {
            bgp_large_community.add( IXP_LC_INFO_SAME_AS_NEXT_HOP );
        } else {
            bgp_large_community.add( IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP );
            accept;
        }
    }

    if filter_has_transit_path() then accept;

    if( bgp_path.len > 64 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_LONG );
        accept;
    }

    allas = [ 61455 ];

    if !(bgp_path.last_nonaggregated ~ allas) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED );
        accept;
    }

    if ( roa_check(roa_v4, net, bgp_path.last) = ROA_VALID ) then {
        bgp_large_community.add( IXP_LC_INFO_RPKI_VALID );
    } else {
        if ( roa_check(roa_v4, net, bgp_path.last) = ROA_INVALID ) then {
            bgp_large_community.add( IXP_LC_INFO_RPKI_INVALID );
            bgp_large_community.add( IXP_LC_FILTERED_RPKI_INVALID );
        } else {
            bgp_large_community.add( IXP_LC_INFO_RPKI_UNKNOWN );
        }
    }

    bgp_large_community.add( IXP_LC_INFO_IRRDB_NOT_CHECKED );

    honor_graceful_shutdown();

    bgp_community.add( (routeserverasn, 65250) );

    accept;
}

# el export strippea nuestras propias communities de filtrado y looking glass.
# Las dos formas: dejar pasar las estandar (routeserverasn, *) filtra menos de
# lo que el operador cree, porque las de control de anuncio y las de marca de
# upstream llegarian al miembro
filter f_export_CDN_192_0_2_18_v4
{
    bgp_large_community.delete( [( routeserverasn, *, * )] );
    bgp_community.delete( [( routeserverasn, * )] );
    accept;
}

protocol bgp pb_CDN_192_0_2_18_v4 from tb_rsclient_v4 {
    description "Miembro AS61455";
    neighbor 192.0.2.18 as 61455;

    ipv4 {
        import filter f_import_CDN_192_0_2_18_v4;
        table t_CDN_192_0_2_18_v4;
        export filter f_export_CDN_192_0_2_18_v4;
    };
}

protocol pipe pp_CDN_192_0_2_18_v4 {
    description "Pipe for Miembro AS61455";
    table master4;
    peer table t_CDN_192_0_2_18_v4;
    import filter f_export_to_master;
    export where ixp_community_filter(61455);
}
# Miembro AS273973 (AS273973) en 192.0.2.11
ipv4 table t_APO_192_0_2_11_v4;

filter f_import_APO_192_0_2_11_v4
prefix set allnet;
ip set allips;
int set allas;
{
    if ( net ~ [ 0.0.0.0/0{25,32} ] ) then {
        bgp_large_community.add( IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG );
        accept;
    }

    if !(avoid_martians4()) then {
        bgp_large_community.add( IXP_LC_FILTERED_BOGON );
        accept;
    }

    if( bgp_path.len < 1 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_SHORT );
        accept;
    }

    if (bgp_path.first != 273973 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS );
        accept;
    }

    allips = [ 192.0.2.11 ];

    if !( from = bgp_next_hop ) then {
        if( bgp_next_hop ~ allips ) then {
            bgp_large_community.add( IXP_LC_INFO_SAME_AS_NEXT_HOP );
        } else {
            bgp_large_community.add( IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP );
            accept;
        }
    }

    if filter_has_transit_path() then accept;

    if( bgp_path.len > 64 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_LONG );
        accept;
    }

    allas = [ 273973 ];

    if !(bgp_path.last_nonaggregated ~ allas) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED );
        accept;
    }

    if ( roa_check(roa_v4, net, bgp_path.last) = ROA_VALID ) then {
        bgp_large_community.add( IXP_LC_INFO_RPKI_VALID );
    } else {
        if ( roa_check(roa_v4, net, bgp_path.last) = ROA_INVALID ) then {
            bgp_large_community.add( IXP_LC_INFO_RPKI_INVALID );
            bgp_large_community.add( IXP_LC_FILTERED_RPKI_INVALID );
        } else {
            bgp_large_community.add( IXP_LC_INFO_RPKI_UNKNOWN );
        }
    }

    allnet = [ 45.170.100.0/24, 45.238.179.0/24 ];

    if ! (net ~ allnet) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_PREFIX_FILTERED );
        bgp_large_community.add( IXP_LC_INFO_IRRDB_FILTERED_STRICT );
        accept;
    } else {
        bgp_large_community.add( IXP_LC_INFO_IRRDB_VALID );
    }

    honor_graceful_shutdown();

    bgp_community.add( (routeserverasn, 65220) );

    accept;
}

# el export strippea nuestras propias communities de filtrado y looking glass.
# Las dos formas: dejar pasar las estandar (routeserverasn, *) filtra menos de
# lo que el operador cree, porque las de control de anuncio y las de marca de
# upstream llegarian al miembro
filter f_export_APO_192_0_2_11_v4
{
    bgp_large_community.delete( [( routeserverasn, *, * )] );
    bgp_community.delete( [( routeserverasn, * )] );
    accept;
}

protocol bgp pb_APO_192_0_2_11_v4 from tb_rsclient_v4 {
    description "Miembro AS273973";
    neighbor 192.0.2.11 as 273973;

    ipv4 {
        import limit 5 action restart;
        import filter f_import_APO_192_0_2_11_v4;
        table t_APO_192_0_2_11_v4;
        export filter f_export_APO_192_0_2_11_v4;
    };
}

protocol pipe pp_APO_192_0_2_11_v4 {
    description "Pipe for Miembro AS273973";
    table master4;
    peer table t_APO_192_0_2_11_v4;
    import filter f_export_to_master;
    export where ixp_community_filter(273973);
}
# PIT Chile v4 (AS64166, upstream)
ipv4 table t_PIT_Chile_v4_192_0_2_5_v4;

protocol bgp pb_PIT_Chile_v4_192_0_2_5_v4 {
    description "PIT Chile v4";
    local as 64166;
    neighbor 192.0.2.5 as 64166;
    passive yes;

    ipv4 {
        import filter {
            bgp_community.add( (64166, 9999) );
            accept;
        };
        export all;
        table t_PIT_Chile_v4_192_0_2_5_v4;
    };
}

protocol pipe pp_PIT_Chile_v4_192_0_2_5_v4 {
    description "Pipe for PIT Chile v4";
    table master4;
    peer table t_PIT_Chile_v4_192_0_2_5_v4;
    import all;
    export where !(bgp_community ~ [(64166, 9999)]);
}
template bgp tb_rsclient_v6 {
    local as routeserverasn;
    source address routeserveraddress6;
    passive yes;

    # le da tiempo al RTR a poblarse antes de que levanten las sesiones
    connect delay time 30;

    # pass through de communities well-known de rfc1997
    interpret communities off;

    ipv6 {
        import table on;
        export all;
    };

    rs client;
}
# Miembro AS25152 (AS25152) en 2001:db8::10
ipv6 table t_KROOT_2001_db8__10_v6;

filter f_import_KROOT_2001_db8__10_v6
ip set allips;
int set allas;
{
    if ( net ~ [ ::/0{49,128} ] ) then {
        bgp_large_community.add( IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG );
        accept;
    }

    if !(avoid_martians6()) then {
        bgp_large_community.add( IXP_LC_FILTERED_BOGON );
        accept;
    }

    if( bgp_path.len < 1 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_SHORT );
        accept;
    }

    if (bgp_path.first != 25152 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS );
        accept;
    }

    allips = [ 2001:db8::10 ];

    if !( from = bgp_next_hop ) then {
        if( bgp_next_hop ~ allips ) then {
            bgp_large_community.add( IXP_LC_INFO_SAME_AS_NEXT_HOP );
        } else {
            bgp_large_community.add( IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP );
            accept;
        }
    }

    if filter_has_transit_path() then accept;

    if( bgp_path.len > 64 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_LONG );
        accept;
    }

    allas = [ 25152 ];

    if !(bgp_path.last_nonaggregated ~ allas) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED );
        accept;
    }

    if ( roa_check(roa_v6, net, bgp_path.last) = ROA_VALID ) then {
        bgp_large_community.add( IXP_LC_INFO_RPKI_VALID );
    } else {
        if ( roa_check(roa_v6, net, bgp_path.last) = ROA_INVALID ) then {
            bgp_large_community.add( IXP_LC_INFO_RPKI_INVALID );
            bgp_large_community.add( IXP_LC_FILTERED_RPKI_INVALID );
        } else {
            bgp_large_community.add( IXP_LC_INFO_RPKI_UNKNOWN );
        }
    }

    bgp_large_community.add( IXP_LC_INFO_IRRDB_NOT_CHECKED );

    honor_graceful_shutdown();

    bgp_community.add( (routeserverasn, 65270) );

    accept;
}

# el export strippea nuestras propias communities de filtrado y looking glass.
# Las dos formas: dejar pasar las estandar (routeserverasn, *) filtra menos de
# lo que el operador cree, porque las de control de anuncio y las de marca de
# upstream llegarian al miembro
filter f_export_KROOT_2001_db8__10_v6
{
    bgp_large_community.delete( [( routeserverasn, *, * )] );
    bgp_community.delete( [( routeserverasn, * )] );
    accept;
}

protocol bgp pb_KROOT_2001_db8__10_v6 from tb_rsclient_v6 {
    description "Miembro AS25152";
    neighbor 2001:db8::10 as 25152;

    ipv6 {
        import limit 6 action restart;
        import filter f_import_KROOT_2001_db8__10_v6;
        table t_KROOT_2001_db8__10_v6;
        export filter f_export_KROOT_2001_db8__10_v6;
    };
}

protocol pipe pp_KROOT_2001_db8__10_v6 {
    description "Pipe for Miembro AS25152";
    table master6;
    peer table t_KROOT_2001_db8__10_v6;
    import filter f_export_to_master;
    export where ixp_community_filter(25152);
}
# Miembro AS273973 (AS273973) en 2001:db8::11
ipv6 table t_APO_2001_db8__11_v6;

filter f_import_APO_2001_db8__11_v6
prefix set allnet;
ip set allips;
int set allas;
{
    if ( net ~ [ ::/0{49,128} ] ) then {
        bgp_large_community.add( IXP_LC_FILTERED_PREFIX_LEN_TOO_LONG );
        accept;
    }

    if !(avoid_martians6()) then {
        bgp_large_community.add( IXP_LC_FILTERED_BOGON );
        accept;
    }

    if( bgp_path.len < 1 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_SHORT );
        accept;
    }

    if (bgp_path.first != 273973 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_FIRST_AS_NOT_PEER_AS );
        accept;
    }

    allips = [ 2001:db8::11 ];

    if !( from = bgp_next_hop ) then {
        if( bgp_next_hop ~ allips ) then {
            bgp_large_community.add( IXP_LC_INFO_SAME_AS_NEXT_HOP );
        } else {
            bgp_large_community.add( IXP_LC_FILTERED_NEXT_HOP_NOT_PEER_IP );
            accept;
        }
    }

    if filter_has_transit_path() then accept;

    if( bgp_path.len > 64 ) then {
        bgp_large_community.add( IXP_LC_FILTERED_AS_PATH_TOO_LONG );
        accept;
    }

    allas = [ 273973 ];

    if !(bgp_path.last_nonaggregated ~ allas) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_ORIGIN_AS_FILTERED );
        accept;
    }

    if ( roa_check(roa_v6, net, bgp_path.last) = ROA_VALID ) then {
        bgp_large_community.add( IXP_LC_INFO_RPKI_VALID );
    } else {
        if ( roa_check(roa_v6, net, bgp_path.last) = ROA_INVALID ) then {
            bgp_large_community.add( IXP_LC_INFO_RPKI_INVALID );
            bgp_large_community.add( IXP_LC_FILTERED_RPKI_INVALID );
        } else {
            bgp_large_community.add( IXP_LC_INFO_RPKI_UNKNOWN );
        }
    }

    allnet = [ 2400:aa::/48 ];

    if ! (net ~ allnet) then {
        bgp_large_community.add( IXP_LC_FILTERED_IRRDB_PREFIX_FILTERED );
        bgp_large_community.add( IXP_LC_INFO_IRRDB_FILTERED_STRICT );
        accept;
    } else {
        bgp_large_community.add( IXP_LC_INFO_IRRDB_VALID );
    }

    honor_graceful_shutdown();

    bgp_community.add( (routeserverasn, 65220) );

    accept;
}

# el export strippea nuestras propias communities de filtrado y looking glass.
# Las dos formas: dejar pasar las estandar (routeserverasn, *) filtra menos de
# lo que el operador cree, porque las de control de anuncio y las de marca de
# upstream llegarian al miembro
filter f_export_APO_2001_db8__11_v6
{
    bgp_large_community.delete( [( routeserverasn, *, * )] );
    bgp_community.delete( [( routeserverasn, * )] );
    accept;
}

protocol bgp pb_APO_2001_db8__11_v6 from tb_rsclient_v6 {
    description "Miembro AS273973";
    neighbor 2001:db8::11 as 273973;

    ipv6 {
        import limit 5 action restart;
        import filter f_import_APO_2001_db8__11_v6;
        table t_APO_2001_db8__11_v6;
        export filter f_export_APO_2001_db8__11_v6;
    };
}

protocol pipe pp_APO_2001_db8__11_v6 {
    description "Pipe for Miembro AS273973";
    table master6;
    peer table t_APO_2001_db8__11_v6;
    import filter f_export_to_master;
    export where ixp_community_filter(273973);
}
